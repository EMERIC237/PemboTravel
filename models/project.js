class Project {
  constructor(id, city, imageUrl, price, description) {
    (this.id = id),
      (this.city = city),
      (this.imageUrl = imageUrl),
      (this.price = price),
      (this.description = description);
  }
}

export default Project;
